export enum TransactionType {
  REVENUE = 'revenue',
  EXPENSES = 'expenses',
}

export enum TransactionStatus {
  OPEN = 'open',
  PAID = 'paid',
}

export enum InvoiceStatus {
  OPEN = 'open',
  CLOSED = 'closed',
}
